{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (on ~/dev)",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${userHome}/dev"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "npm: compile"
    }
  ]
}
